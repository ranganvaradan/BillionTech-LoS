package com.los.core.creditintelligence.policystudio.runtime.canonicalconfig;

/**
 * W11.2 typed resolution result. Never silently falls back to another authority.
 */
public enum CanonicalResolutionStatus {
    RESOLVED,
    NOT_RESOLVABLE
}
