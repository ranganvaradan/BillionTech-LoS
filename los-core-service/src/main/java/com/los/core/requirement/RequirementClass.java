package com.los.core.requirement;

/**
 * Planner classification (LOCKED vocabulary from Workflow Architecture Design Lock).
 */
public enum RequirementClass {
    ALREADY_AVAILABLE,
    DERIVABLE,
    AUTO_SOURCE,
    CUSTOMER_PROVIDED,
    MANUAL_REVIEW,
    UNAVAILABLE_BLOCKER
}
