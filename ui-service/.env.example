# Optional. Defaults to same-origin /api/v1 (use Vite dev proxy to localhost:8080).
# VITE_API_BASE_URL=https://your-api-host/api/v1
# VITE_BASE_PATH=/
# VITE_STAGING_DEMO=true
# Required only when the target core has a non-blank CREDIT_INTELLIGENCE_INTERNAL_TOKEN:
# VITE_CREDIT_INTELLIGENCE_INTERNAL_TOKEN=

