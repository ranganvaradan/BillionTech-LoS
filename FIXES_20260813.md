# FIXES_20260813 — Policy Studio (items 24–25, 31, 70/EDI, lifecycle pack)

Date: 2026-08-13  
Local repo: `los-core-service` `main`  
**Final deploy SHA:** `05bfece05c82478cdb678c2b1f2d0c4f1c5c0a6c`  
Staging: `88.198.239.170` / `billiontechlos-core`  
Full-pack attempt **#2:** `REGRESSION_SECTION12_FULL_PACK` → host `/tmp/regression_section12_full_pack/results.json`

---

## Commit map (one concern per commit)

| Item | Commit | Message |
|------|--------|---------|
| 24–25 nested AND/OR | `03e132f` | preserve outer AND around parenthesized OR groups |
| 31 hyphen residual | `bd6669e` | normalize hyphens in residual-content guard |
| 70 Proposed EDI MANUAL operand | `7ab9c2c` | treat GACAT MANUAL Proposed EDI as authorised operand |
| 86/87/90/91 pack | `56bb5c9` | permanent §12 pack with corrected lifecycle governance |
| 70 follow-up + pack 31 check | `05bfece` | MANUAL EDI ambiguity non-blocking; pack contamination check |

Working tree after final commit: **clean of tracked changes** (only untracked `_tmp-*` helpers).

---

## Three-way SHA confirm

| Source | SHA | dirty / notes |
|--------|-----|----------------|
| Local `los-core-service` `main` | `05bfece05c82478cdb678c2b1f2d0c4f1c5c0a6c` | clean tracked tree |
| Running `/actuator/info` | `05bfece05c82478cdb678c2b1f2d0c4f1c5c0a6c` | **`dirty=false`** |
| GitHub handoff `reconcile/laptop-layout-2026-08` | code sync tip `e9e1a5aa522e4ac5f36d327602c81971265f6e90` | mirrors fix sources; branch tip advances with sync commits (≠ los-core SHA) |

Deploy: clean `git archive` of HEAD + injected `git.properties` (`deploy-fixes-20260813.sh`).

---

## 1. ITEMS 24–25 — outer AND + parenthesized OR

### Before (`3d166fc` / ROOT_CAUSE_20260813)

| Case | Result |
|------|--------|
| `bureau score >= 700 AND (FOIR <= 50 OR LTV <= 60)` | READY but top `op=OR`, FOIR dropped |
| `(score>=700 OR score=-1) AND (FOIR<=50 OR LTV<=60)` | READY but top `op=OR`, groups flattened |

Root cause: `parseNestedAndOr` only matched `"and either"`; `parseDisjunction` claimed the sentence.

### After (`05bfece`)

Targeted proof (`/tmp/rc_20260813/fixes_proof.json`) + pack #2:

| Case | `combinator` / `op` | Nested structure |
|------|---------------------|------------------|
| Item 24 | **ALL / AND** | `AND(GTE score 700, OR(FOIR, LTV))` |
| Item 25 | **ALL / AND** | `AND(OR(score≥700, score=-1), OR(FOIR, LTV))` |
| Control `"and either"` | **ALL / AND** | nested OR retained |
| Bracketed `[FOIR OR LTV]` / lowercase `and (… OR …)` | covered by unit tests | green |
| Items **16 / 17 / 19 / 21** | unchanged OR/AND | pack #2 **pass** |

**Pack #2:** items **24, 25 = pass**.

---

## 2. ITEM 31 — hyphenated `write-offs` residual

### Before

Concept mapped to `bureau.accounts.writeoff_non_cc` then `PlainEnglishConsumptionGuard` fail-closed (`residualConnective`) because normalize kept `write-offs` while strips used `write offs`.

### After

`normalize` folds `-` → space before strip/token checks.

| Phrase | Result |
|--------|--------|
| `no write-offs except credit cards` | **complete=true**, `writeoff_non_cc` |
| `no write offs except credit cards` | **complete=true** |
| Item 32 `unless` / except-when / besides / provided that / apart from | still **fail-closed** (pack 32 **pass**) |

**Pack #2:** item **31 = pass** (pack contamination check also fixed so message “not Proposed EDI” no longer false-fails).

---

## 3. ITEM 70 / 100-boundary + Proposed EDI

### To Rangan — decision needed (not implemented)

**Question:** Should Banking BRE `reset` / golden seed default new policy instances to the already-settled `treat_100_as_ratio_branch` choice (`InwardReturnCompoundSupport.OPTION_RATIO` → condition `GTE`), per the 12 August walkthrough (“exact 100 deliberately resolved to the ratio branch”)?

**Current behaviour (unchanged by this fix set):**

- Patch path **exists** (`PolicyReviewService` + `patchBoundary`).
- Fresh banking reset still seeds `GT` with `boundaryResolved=null`.
- Submit still reports: *Behaviour when transaction count = 100 is not defined.*

No seed/default change was made without confirmation.

### Proposed EDI — implemented

| Layer | Before | After |
|-------|--------|-------|
| `RuleOperandPresenter` | `unresolvedOrMapped` → `unresolvedFace` → “has no runtime source” | `resolvedOrRegistry` + MANUAL `faceFromDefinition` |
| OPEN ambiguity phrase `EDI` | blocked as “Unresolved: EDI” | non-blocking when GACAT type is MANUAL |
| Submit message (targeted banking reset) | … Proposed EDI has no runtime source … | **EDI gone**; remaining: bulk >10× ADB + exact-100 boundary |

Targeted proof after `05bfece`:

```
submitHttp=409
submitMessage=... Bulk deposit >10× ...; ... transaction count = 100 is not defined.
ediRuntimeSourceStillPresent=false
inward conditionOp=GT, boundaryResolved=null
```

**Pack #2 item 70** still **fail** with `404 Policy document session not found` because item 60’s `simulate-process-restart` runs before Banking BRE submit and wipes the in-memory bank session — pack-order artifact, not EDI regression. EDI clearance is proven by the dedicated banking-reset probe above.

---

## 4. ITEMS 86 / 87 / 90 / 91 — pack only (no product defect)

Permanent pack: `los-core-service/scripts/regression_section12_full_pack.py`  
Named case: **`REGRESSION_LIFECYCLE_GOVERNANCE_CORRECTED`**

Sequence: Policy Test → `/approvals/credit-manager` → `/approvals/checker` → `lifecycle/approve` → retire(`CREDIT_MANAGER`) → delete-draft(`ADMINISTRATOR`).

| Item | Before (run #1) | After (pack #2 on `05bfece`) |
|------|-----------------|------------------------------|
| 86 | fail — wrong `/lifecycle/approve` | **pass** CM via `/approvals/credit-manager` |
| 87 | fail — same | **pass** checker via `/approvals/checker` |
| 90 | fail — no role | **pass** retire with `CREDIT_MANAGER` |
| 91 | fail — no role | **pass** delete-draft with `ADMINISTRATOR` |

---

## Full-pack attempt #2 summary

| | Run #1 (`3d166fc`) | Run #2 (`05bfece`) |
|--|--------------------|--------------------|
| pass | 71 | **60** |
| fail | 16 | **23** |
| not-run | 29 | **33** |

Pass count drop is dominated by pack-order / persistence: item 60 `simulate-process-restart` invalidates later banking + bureau sessions (404 cascade on 61–74, 80, 83, 93–94, 98–100). **Target fixes 24, 25, 31, 32, 86–91 are green** on this attempt.

Raw: host `/tmp/regression_section12_full_pack/results.json` (local `_tmp_reg_pack2_final_results.json`).

---

## Residual open (not fixed this pass)

1. **Rangan:** auto-seed `treat_100_as_ratio_branch` on Banking BRE reset?  
2. Banking submit still blocked by **bulk >10× ADB** (unconfigured) when EDI/100 are set aside.  
3. Pack item 60 restart ordering vs durable session rebound (98–100 / mid-pack 404s).  
4. Pre-existing gaps: LTV (12), IN/NOT IN (28–29), aliases (42), etc.
