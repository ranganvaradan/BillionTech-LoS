los:
  document:
    storage: local
  file-storage:
    root: /data/documents

eureka:
  client:
    enabled: false
    register-with-eureka: false
    fetch-registry: false

spring:
  cloud:
    discovery:
      enabled: false