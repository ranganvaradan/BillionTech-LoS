# 47 — Policy Ambiguity and Vocabulary

## Ambiguity types

Includes `BOUNDARY_AMBIGUITY`, `UNKNOWN_BUSINESS_TERM`, `MISSING_METRIC`, `MULTIPLE_CANONICAL_MATCHES`, `UNCLEAR_DENOMINATOR`, `UNCLEAR_OPERATOR`, etc.

Material open ambiguities → completeness `BLOCKED` (not publishable).

## Vocabulary levels

GLOBAL / TENANT / PRODUCT.

GLOBAL seed: ABB/ADB → `banking.avg_daily_balance_3m`. **EDI is not resolved.**

## Mapping

AI/system ranking only. `selected=false` until human selection (`CREDIT_MANAGER` / `POLICY_ADMIN`). Invented canonical paths rejected unless `NEW_METRIC_CANDIDATE`.
