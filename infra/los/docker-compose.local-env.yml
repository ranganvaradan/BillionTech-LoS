# Local URLs for LOS stack.

services:
  los-core:
    environment:
      PLP_BASE_URL: http://host.docker.internal:8180
      LOS_PUBLIC_API_BASE_URL: http://localhost:8080/los
      LOS_BORROWER_UI_URL: http://localhost:8080/los/borrower
